# Freeze Note — `meridian-p1.0.3` (2026-07-20)

**Tag:** `meridian-p1.0.3` (annotated) · **Anchored at:** master merge of `feat/eval-mechanism-gate` (`ebf2849`)
**Supersedes as the working baseline:** `meridian-p1.0.2`. The research baseline `eval-baseline-v3`
remains separate, immutable, and untouched.

---

## What this baseline is

`meridian-p1.0.3` freezes the tree at the point where the **deterministic mechanism eval gate** is
merged. It is additive acceptance infrastructure: it adds a way to *validate* releases, and changes
no product behaviour, no frozen algorithm, and no evaluation baseline.

Contents added over `meridian-p1.0.2`:

- **`callosum eval-mechanism`** + `scripts/eval_mechanism.sh` — a deterministic, CI-safe acceptance
  gate that makes zero cloud-LLM calls. Candidate retrieval, gold-seeded 2-hop traversal, and RBAC
  fail-closed, computed by *calling* the frozen retrieval stages. Exits non-zero on any regression.
- **`evaluate.evaluate_mechanism()` / `write_mechanism_csv()`** and 6 DB-free gate-logic tests.
- **Design + postmortem** under `docs/`, and the recorded gate baseline `eval/mechanism.csv`.

Gate baseline (tenant stack, no LLM): candidate **22/22**, traversal **21/21 (100%)**, RBAC **1/1**.

## Why it is safe to freeze here

- **No frozen file touched.** `ingest.py`, `extract.py`, `retrieve.py`, `store.py`,
  `schema/postgres.sql` are byte-identical to `meridian-p1.0.2`. The D-001 ban-test is green.
- **The LLM eval path is unchanged.** `evaluate()`, `eval.sh`, `eval_tenant.sh` are untouched, so
  `eval-baseline-v3` reproduction is unaffected by construction and was not re-run.
- **Additive only.** New functions + one subcommand; nothing existing was modified in behaviour.
- Fast suite 78 passed.

## Freeze rules going forward (unchanged from p1)

- The tag is immutable and never moves; all future work builds *on* it.
- The next master commit is either a real reproduced bug fix vs `meridian-p1.0.3` or the first
  accepted next-step item — nothing in between.
- A change to the frozen core still requires a *measured* eval justification. The mechanism gate is
  now the fast, deterministic first line of that evidence.

## Next

P2 continues one measured step at a time. Queued owner-track item: the `entity_conflict` UNIQUE-key
migration (add `workspace_id` to the uniqueness constraint). The mechanism gate is now available to
guard that change deterministically.
