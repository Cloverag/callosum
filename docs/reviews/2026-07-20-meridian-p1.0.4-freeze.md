# Freeze Note — `meridian-p1.0.4` (2026-07-20)

**Tag:** `meridian-p1.0.4` (annotated) · **Anchored at:** master merge of `feat/entity-conflict-workspace-uq` (`6b0d3a0`)
**Supersedes as the working baseline:** `meridian-p1.0.3`. The research baseline `eval-baseline-v3`
remains separate, immutable, and untouched.

---

## What this baseline is

`meridian-p1.0.4` freezes the tree at the point where the `entity_conflict` unique key is scoped to
the workspace. It is a one-migration tenant-isolation schema patch: it changes no code, no product
behaviour, no frozen algorithm, and no evaluation baseline.

Change over `meridian-p1.0.3`:

- **Migration `0006_conflict_workspace_uq`** — unique key widened from `(name_a, type_a, name_b,
  type_b)` to `(workspace_id, name_a, type_a, name_b, type_b)`, closing a latent cross-tenant
  collision (the index is enforced below RLS, so identical pairs across workspaces collided).
- **Regression test** proving allowed-across-workspaces / rejected-within-workspace.

## Why it is safe to freeze here

- **No frozen file touched.** `ingest.py`, `extract.py`, `retrieve.py`, `store.py`,
  `schema/postgres.sql` are byte-identical to `meridian-p1.0.3`. `entity_conflict` lives only in
  Alembic; the frozen SQL schema does not define it.
- **No code change** — detection already stamped `workspace_id` (F2 / `meridian-p1.0.1`).
- **Fully verified**: fresh-volume migrate `0001→0006`, upgrade/downgrade reversible, integration
  10/10, fast suite 78, mechanism gate PASSED (22/22, 21/21, 1/1).

## Freeze rules going forward (unchanged)

- The tag is immutable and never moves; all future work builds *on* it.
- The next master commit is either a real reproduced bug fix vs `meridian-p1.0.4` or the first
  accepted next-step item — nothing in between.
- A frozen-core change still requires a *measured* eval justification; the mechanism gate is the
  fast deterministic first line of that evidence.

## Next

The p1 two-store tenant-isolation hardening line (`p1.0.1` → `p1.0.4`) is **complete**. The next
owner milestone is **ROADMAP P2 — durable product domain + migrations** (workspace, meetings,
agendas, decisions): the first product-forward checkpoint since P1, now standing on a verified
isolation base and guarded by a deterministic acceptance gate.
