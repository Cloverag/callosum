# R8-R13 evaluation review — live run 2026-07-16

Completed copy of `docs/templates/r8-r12-evaluation-review.md`. Data is filled from a
live run; **Decision fields are intentionally left open for the reviewer** (this record
does not accept any checkpoint or authorize P0).

## Run identity

- Review date/time: 2026-07-16
- Reviewer(s): _pending_
- Repository commit SHA and branch: `master` (trunk after R8-R13 handoff merge)
- Comparison baseline/tag: `eval-baseline-v2` (`932f15a`)
- Provider, chat model, embedding model: ollama · gpt-oss:120b-cloud · bge-m3
- Prompt version / ontology version: planner prompt (candidate-grounding) / ontology **v3** (`ALIAS_OF`)
- Python / OS / Docker: 3.12 / Linux (Arch) / docker compose
- Corpus documents: Meetings 12-16 (s1), finance & sales FY27 forecasts (s1), 8 messy docs
  incl. `.docx`/`.pdf`/`.vtt`/`.md` (s1, one restricted s3), compensation review (s3) — 16 total
- Commands run: `bash scripts/eval.sh` (reset → ingest --no-extract → seed-eval → eval)
- Was `scripts/eval.sh` allowed to delete local volumes? **Yes** (clean-environment run)

## Preconditions

- [x] Provider reachable (Ollama available — the handoff's release blocker is cleared for this run).
- [x] Fresh Postgres and Neo4j environment used (`docker compose down -v`).
- [x] `eval/results.csv` was appended, not replaced.
- [x] Fast deterministic suite passed: **36 passed, 1 skipped, 5 deselected**.
- [x] No unreviewed changes to the frozen core in this run (trunk code only; the reconcile added no core edits).

## Results summary

New rows appended to `eval/results.csv`; generated `eval/results.md` in tree.

| Metric | Baseline (v2) | Current | Delta | Interpretation |
|---|---:|---:|---:|---|
| Grounding accuracy | 85% | 71% (15/21) | −14 | recall dips with a larger, harder entity space |
| GER | 15% | 29% | +14 | mirror of the above |
| Grounding precision / false positives | 50% | 33% (1/3) | −17 | **degrades with scale — the headline weakness** |
| Candidate recall (R12) | n/a | not isolated this run | — | candidate list feeds the planner; see per-question `grounded_to` in CSV |
| Candidate/planner latency (R12) | n/a | not separately timed | — | needs an instrumented run |
| Traversal accuracy given grounding | 100% | 100% | 0 | engine remains correct; bottleneck is grounding |
| Vector-only graph-fact recall | n/a (graph-exclusive) | — | — | vector arm has no graph facts by construction |
| Hybrid graph-fact recall (mean, graph Qs) | ~100% | 83% | −17 | capped by the 71% grounding recall |
| RBAC negative cases | pass | 2/2 pass | 0 | restricted email + comp correctly withheld |

Per-stratum hybrid graph-fact recall: relational 100 · multi_hop 50 · temporal 75 ·
**aliases 100** · conflict 100 · **coreference 0** · messy_email 100 · grounding_adv 88.

## Checkpoint evidence and decision

### R8 — aliases and resolution
- **Aliases recall 100%.** Candidate grounding + `ALIAS_OF` (v3) resolves alias spellings
  on the full corpus. Alias evidence/provenance preserved (reviewed-claim model; spelling
  stays its own node).
- Decision: **Pass / Fail / Inconclusive — _reviewer_**

### R9 — conflicting evidence
- Conflict recall 100% (both FY27 forecasts retrieved, independently sourced), but hybrid
  answer 1/2 — synthesis does not reliably present the disagreement. Retrieval correct;
  presentation gap. Both claims remain permission-filtered.
- Decision: **Pass / Fail / Inconclusive — _reviewer_**

### R10 — context-dependent references (coreference)
- **Coreference recall 0%.** "it" grounded to `Board Meeting 16` instead of the referenced
  plan. No coreference stage exists; contextual references fail. Hybrid answer-correctness
  on this stratum is lenient string-matching, not resolution.
- Decision: **likely Fail — _reviewer confirms_**

### R11 — messy documents
- `.docx`/`.pdf`/`.vtt`/`.md` ingested; messy_email recall 100%; restricted email withheld
  correctly under RBAC. DOCX visual QA recorded separately (`docs/docx-visual-qa.md`).
- Decision: **Pass / Fail / Inconclusive — _reviewer_**

### R12 — candidate grounding and abstention
- Candidate grounding active (permission-scoped names to the planner; model-emitted names
  outside the candidate set removed). Helped alias recall (100%) but **did not fix
  precision** (33%, 2/3 negatives false-positive). Candidate recall/latency not isolated
  this run — a dedicated instrumented run is recommended before acceptance.
- Decision: **Inconclusive — needs an R12-instrumented run — _reviewer_**

## Reviewer decision (overall)

- R8-R13 acceptance: **_pending reviewer_**. This run supplies the live evidence the
  handoff required; it does not by itself accept any checkpoint or authorize P0.
- Recommended next: an R12-instrumented run (candidate recall + latency), a coreference
  stage before R10 can pass, and abstention work targeting the precision regression.
