# Copy to .env and fill in. .env is gitignored.

# console.anthropic.com → API keys
ANTHROPIC_API_KEY=sk-ant-...

# dashboard.voyageai.com → API keys (embeddings; 200M tokens free)
VOYAGE_API_KEY=pa-...

# Defaults match docker-compose.yml — override only if you changed it.
POSTGRES_DSN=postgresql://callosum:callosum@localhost:5433/callosum
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=callosum123
